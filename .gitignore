node_modules
dist
dist-ssr
coverage
*.local
.env
.env.*
!.env.example

# 에디터 / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 빌드 캐시
*.tsbuildinfo
.vite

# Claude Code 가 만드는 임시 워크트리.
# .claude/ 를 통째로 무시하면 안 된다 — launch.json 은 추적 대상이다.
# 여기 남은 사본을 vitest 가 같이 긁어서 무관한 실패가 섞인 적이 있다.
.claude/worktrees/
