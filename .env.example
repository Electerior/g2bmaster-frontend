# 백엔드(Spring) 주소. 비워두면 같은 오리진의 /api 를 그대로 호출한다.
# 개발 중에는 vite.config.ts 의 프록시가 /api → VITE_PROXY_TARGET 으로 넘겨주므로
# 보통 이 값은 비워두고 VITE_PROXY_TARGET 만 설정한다.
VITE_API_BASE_URL=

# vite dev server 가 /api 를 프록시할 백엔드 주소 (기본값 http://localhost:8080)
VITE_PROXY_TARGET=http://localhost:8080

# 앱 API 키. 저장 공고 등 쓰기 경로(@RequireAppAuth)를 백엔드가 이 키로 가린다.
# 비우면 헤더를 붙이지 않는다 — 백엔드도 APP_API_KEY 가 비어 있으면 인증을 끄므로(개발 모드)
# 로컬에서는 그대로 비워 두면 된다. 운영에서 백엔드 APP_API_KEY 를 켜면 같은 값을 여기에 넣는다.
VITE_APP_API_KEY=

# 공고 단순 요약(POST /api/notice-summary) 호출 여부.
# 'true'이면 계획·사전규격·입찰공고 서랍을 열 때 실제 요약 API를 호출한다.
# 백엔드 AI_ENABLED(g2b.ai.enabled)와 함께 켜며, LLM 실패도 HTTP 200 fallback으로 표시한다.
# scan-attachments 는 로컬 첨부 색인 조회라 이 플래그와 무관하다.
VITE_AI_ENABLED=

# 베타 모집 랜딩(/beta)의 접수처 — 구글 Apps Script 웹앱 주소.
#
# 평소에는 건드리지 않는다. 기본 주소가 src/api/beta.ts 에 들어 있어서 저장소만 받아도
# 접수가 시트로 간다 — 서버마다 .env 를 두지 않으려고 그렇게 뒀다.
#
# 주석을 풀 때는 값을 반드시 채운다. 빈 값(VITE_BETA_SHEET_URL=)은 "설정 안 함"이 아니라
# "Spring 의 /api/beta/* 로 보내라"는 뜻이고, 그 접수 API 는 아직 배포돼 있지 않다.
# 다른 시트로 임시로 돌릴 때만 쓴다. 배포 방법은 docs/beta-signup.gs 맨 아래 참고.
# VITE_BETA_SHEET_URL=
